```bash
pip install requests python-dotenv pandas
```
or 
```bash
conda install requests pandas python-dotenv
```

DM Raymond for the .env file if you want the API.
For Graders, generate an API key for LocationIQ
```bash
https://docs.locationiq.com/docs/
```
and place .env file in root directory 
```
LOCATIONIQ_API_KEY = "YOUR_API_KEY_HERE"
```
